package com.celebratehub.repository;

import com.celebratehub.model.AccommodationPlace;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccommodationPlaceRepository extends JpaRepository<AccommodationPlace, Long> {
}
